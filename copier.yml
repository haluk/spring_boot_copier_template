artifact:
  type: str
  help: "Artifact name (e.g. demo)"
  default: "demo"

group_suffix:
  type: str
  help: "Group suffix (e.g., com)"
  default: "com"

group_name:
  type: str
  help: "Group name (e.g., company)"
  default: "company"

auth0_issuer_uri:
  type: str
  help: "Include 'https://' and omit the trailing '/'"

auth0_client_id:
  type: str

auth0_client_secret:
  type: str
