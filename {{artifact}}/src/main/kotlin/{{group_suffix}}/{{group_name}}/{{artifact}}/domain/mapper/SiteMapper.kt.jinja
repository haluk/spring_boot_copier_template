package {{group_suffix}}.{{group_name}}.{{artifact}}.domain.mapper

import {{group_suffix}}.{{group_name}}.{{artifact}}.domain.dto.SiteDto
import {{group_suffix}}.{{group_name}}.{{artifact}}.domain.model.Site
import org.mapstruct.*

@Mapper(
    unmappedTargetPolicy = ReportingPolicy.IGNORE,
    componentModel = MappingConstants.ComponentModel.SPRING,
    uses = [ClientMapper::class]
)
abstract class SiteMapper : GenericMapper<Site, SiteDto> {

    abstract override fun toEntity(dto: SiteDto): Site

    abstract override fun toDto(entity: Site): SiteDto

    @BeanMapping(nullValuePropertyMappingStrategy = NullValuePropertyMappingStrategy.IGNORE)
    abstract override fun partialUpdate(siteDto: SiteDto, @MappingTarget entity: Site)
}
