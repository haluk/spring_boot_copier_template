package {{group_suffix}}.{{group_name}}.{{artifact}}.config

import {{group_suffix}}.{{group_name}}.{{artifact}}.config.pagination.ClippedPageableResolver
import org.springframework.context.annotation.Configuration
import org.springframework.web.method.support.HandlerMethodArgumentResolver
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer

@Configuration
class WebConfig(
    private val clippedPageableResolver: ClippedPageableResolver
) : WebMvcConfigurer {

    override fun addArgumentResolvers(resolvers: MutableList<HandlerMethodArgumentResolver>) {
        resolvers.add(clippedPageableResolver)
    }
}
